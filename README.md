# hotelManagement
1. Activate the environment

$ . venv/bin/activate
  
2. Install Flask

$ pip install Flask

3. Run The Application

$ export FLASK_APP=flaskr
$ export FLASK_ENV=development
$ flask run
